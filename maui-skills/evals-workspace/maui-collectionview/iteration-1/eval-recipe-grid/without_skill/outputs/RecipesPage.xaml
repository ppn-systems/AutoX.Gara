<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="RecipeApp.Views.RecipesPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:RecipeApp.Converters"
    xmlns:models="clr-namespace:RecipeApp.Models"
    xmlns:vm="clr-namespace:RecipeApp.ViewModels"
    Title="Recipes"
    x:DataType="vm:RecipesViewModel">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:IsSelectedConverter x:Key="IsSelectedConverter" />

            <!-- Card border color: accent when selected, transparent otherwise -->
            <Style x:Key="CardBorderStyle" TargetType="Border">
                <Setter Property="StrokeShape" Value="RoundRectangle 12" />
                <Setter Property="StrokeThickness" Value="2" />
                <Setter Property="Stroke" Value="Transparent" />
                <Setter Property="Shadow">
                    <Shadow
                        Brush="Black"
                        Offset="0,2"
                        Opacity="0.12"
                        Radius="8" />
                </Setter>
                <Setter Property="Margin" Value="6" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">

        <!-- Header -->
        <Label
            Grid.Row="0"
            FontAttributes="Bold"
            FontSize="22"
            Margin="16,16,16,8"
            Text="My Recipes" />

        <!-- 2-column recipe grid -->
        <CollectionView
            Grid.Row="1"
            ItemsSource="{Binding Recipes}"
            SelectionMode="None">

            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    Orientation="Vertical"
                    Span="2"
                    HorizontalItemSpacing="4"
                    VerticalItemSpacing="4" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Recipe">
                    <!--
                        The outer Border acts as the "card". Its stroke colour is
                        driven by a MultiBinding that compares the current item to
                        the SelectedRecipe on the ViewModel.
                    -->
                    <Border Style="{StaticResource CardBorderStyle}">

                        <Border.Stroke>
                            <MultiBinding Converter="{StaticResource IsSelectedConverter}">
                                <!-- value[0] = this item -->
                                <Binding Path="." />
                                <!-- value[1] = the currently selected recipe -->
                                <Binding Path="SelectedRecipe" Source="{RelativeSource AncestorType={x:Type vm:RecipesViewModel}}" />
                            </MultiBinding>
                        </Border.Stroke>

                        <!-- Tap gesture fires the SelectRecipeCommand on the ViewModel -->
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding SelectRecipeCommand, Source={RelativeSource AncestorType={x:Type vm:RecipesViewModel}}}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>

                        <Grid RowDefinitions="160,Auto,Auto" BackgroundColor="{AppThemeBinding Light=White, Dark=#1E1E1E}">

                            <!-- Recipe photo -->
                            <Image
                                Grid.Row="0"
                                Aspect="AspectFill"
                                HeightRequest="160"
                                Source="{Binding PhotoUrl}" />

                            <!-- Recipe name -->
                            <Label
                                Grid.Row="1"
                                FontAttributes="Bold"
                                FontSize="14"
                                LineBreakMode="TailTruncation"
                                Margin="10,8,10,2"
                                MaxLines="2"
                                Text="{Binding Name}" />

                            <!-- Prep time -->
                            <HorizontalStackLayout
                                Grid.Row="2"
                                Margin="10,0,10,10"
                                Spacing="4">
                                <Label
                                    FontSize="12"
                                    Text="&#x23F1;"
                                    TextColor="{AppThemeBinding Light=#666, Dark=#AAA}" />
                                <Label
                                    FontSize="12"
                                    Text="{Binding PrepTimeDisplay}"
                                    TextColor="{AppThemeBinding Light=#666, Dark=#AAA}" />
                            </HorizontalStackLayout>

                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
